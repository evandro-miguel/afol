# Temporary Workspace

Use `.agents/tmp/` for temporary files that do not fit the durable scaffold structure yet.

Rules:

- non-canonical only
- safe to delete or replace
- never treat content here as final evidence
- move durable artifacts into `.agents/wb/`, `.agents/arc/`, `.agents/a-docs/`, or another governed location once they become real project assets
