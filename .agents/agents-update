#!/bin/bash
# Agentic System Update CLI wrapper
# Usage: ./.agents/agents-update [command] [options]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "${SCRIPT_DIR}/scripts/agents-update/agents-update.py" "$@"
