[project]
name = "agents-scripts"
version = "0.1.0"
description = "Operational scripts for .agents system"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]

[dependency-groups]
dev = []
