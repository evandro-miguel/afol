---
doc_type: report
id: 260604_2243_merge-pr20-pr24_report_01
theme: merge-pr20-pr24
status: final
owners:
- build
created_at: 2026-06-04 22:46:30-03:00
updated_at: '2026-06-04T22:57:50-03:00'
roadmap_feature: F-pr20-pr24
parent_spec: merge-prs
links:
  plan: 260604_2243_merge-pr20-pr24_plan_01
  task: 260604_2243_merge-pr20-pr24_task_01
---

# Report: merge PRs 20-24

## Summary

- Integrated PRs `#20`, `#23`, `#22`, `#21`, and `#24` into
  `main_dev_merge/pr20-pr24`.
- Resolved semantic duplicate `rule` doc type entries after PR `#22` and `#21`.
- Reviewed Gemini comment area from PR `#23`; consolidated repeated
  `lib.workflow_manifest` imports in both `agents-new.py` files without behavior
  change.
- `changed_files.txt` remains removed and has no repo references.

## Delivered Changes

- PR `#20`: repository hygiene/formatting, bootstrap/test mirror cleanup,
  `.agents/tmp/` ignored, `changed_files.txt` removed.
- PR `#21`: `rule` doc type accepted by docs linter mirrors.
- PR `#22`: rule markdown frontmatter completed with `doc_type: rule`.
- PR `#23`: broader script/test hygiene and formatting integrated.
- PR `#24`: runtime lockfiles updated for `starlette 1.0.1`.
- Manual merge cleanup: duplicate `rule` entries removed and
  `lib.workflow_manifest` imports consolidated in both `agents-new.py` mirrors.
- PR body drafted at
  `.agents/wb/260604_2243_merge-pr20-pr24/260604_2243_merge-pr20-pr24_pr_body.md`.

## Verification

- Focused checks passed:
  - `rg -n '"rule"' .agents/scripts/agents-lint-docs.py src/project-template/.agents/scripts/agents-lint-docs.py`
  - `rg -n '^doc_type: rule$' .agents/rules src/project-template/.agents/rules`
  - `rg -n 'changed_files\.txt' .`
- Required gates passed:
  - `just lint-scripts`
  - `just test-scripts-all` (`589 passed`, coverage `83.36%`)
  - `just lint-runtime`
  - `just test-runtime` (`39 passed`)
  - `just lint` (`24` docs, `0` issues)
  - `just agents-all`
  - `git diff --check`

## Session Review Results

- `verify-tasks`: not run in doc-updater pass; final auditor owns closure
  verification.
- Session review: `./.agents/agents status --session 260604_2243_merge-pr20-pr24`
  before this pass showed plan/log/report format gaps; this pass repaired the
  missing sections.
- Confidence: High.
- Gaps addressed: stale coverage/doc-count values, missing workbench format
  sections, missing PR body, missing docs evidence id.

## Memory Promotion

- Decision: none.
- Source artifacts: `260604_2243_merge-pr20-pr24_report_01`,
  `260604_2243_merge-pr20-pr24_pr_body`.
- Reason: merge evidence is repo-local and tied to this PR integration; no
  cross-repo lesson promoted.

## Notes

- Workbench CLI could not create the requested session because local feature id
  validation rejects `F-pr20-pr24`. Fallback artifacts were created manually
  under `.agents/wb/260604_2243_merge-pr20-pr24`.
- `just agents-all` regenerated structure/index docs during validation; those
  generated map/index deltas were reverted because they were not required for
  this integration and `docs/map` should not be updated without need.
- First independent auditor failed closure because generated docs drift was
  still present and the report implied `docs/map` was untouched. Corrective
  build pass restored:
  `docs/arc/SPECS/INDEX.md`, `docs/knowledge/INDEX.md`,
  `docs/map/structure/README.md`, `docs/map/structure/backend.md`,
  `docs/map/structure/data.md`, and `docs/map/structure/tests.md`.
- Push to `origin/main_dev` failed as non-fast-forward. `origin/main_dev`
  contains a much larger unrelated integration history, so force-push was not
  used and that branch was not merged into this scoped PR integration branch.
- Safe PR route: head `main_dev_merge/pr20-pr24`, base `main`.

## Risks / Follow-ups

- Final auditor Gibbs executed and found only stale workbench metadata. This pass corrected that metadata and recorded evidence E-20260604225750742719142.
- `origin/main_dev` remains ahead/divergent from direct push target; do not
  push to it for this scoped integration.
- No `docs/map` update made; integration did not require canonical map refresh.
