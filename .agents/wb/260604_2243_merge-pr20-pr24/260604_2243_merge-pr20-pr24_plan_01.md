---
doc_type: plan
id: 260604_2243_merge-pr20-pr24_plan_01
theme: merge-pr20-pr24
status: final
owners:
- build
workstream_intent: delivery
artifact_purpose: Execute integration of upstream PRs 20-24.
created_at: 2026-06-04 22:43:46-03:00
updated_at: '2026-06-04T22:57:50-03:00'
roadmap_feature: F-pr20-pr24
parent_spec: merge-prs
links:
  task: 260604_2243_merge-pr20-pr24_task_01
repo: agentic-standard-folder
branch: main_dev_merge/pr20-pr24
---

# Plan: merge PRs 20-24

## Objective

- Integrate upstream PRs `#20` through `#24` into
  `main_dev_merge/pr20-pr24` with conflict cleanup, full repo gates, workbench
  evidence, and reusable PR body.

## Facts

- Base branch: `origin/main`.
- Executor branch: `main_dev_merge/pr20-pr24`.
- PR refs fetched: `origin/pr/20` through `origin/pr/24`.
- Workbench CLI failed before session creation because local contract rejects
  `F-pr20-pr24` and expects `^F-[0-9]{2,3}$`.

## Scope

- Merge PRs in order: `#20`, `#23`, `#22`, `#21`, `#24`.
- Resolve doc type rule conflicts and stale references.
- Run focused checks and required gates.
- Commit local integration only after final audit passes. Create PR from
  `main_dev_merge/pr20-pr24` to `main`; do not push to `main_dev`.

## Success Criteria

- PRs `#20`, `#21`, `#22`, `#23`, and `#24` are represented in the integration
  branch.
- `VALID_DOC_TYPES` contains one `rule` entry in both mirrored lint scripts.
- Rule markdown frontmatter includes `doc_type: rule` where applicable.
- `changed_files.txt` is removed and has no live repo references.
- Required gates pass.
- Workbench report and PR body summarize changes, gates, and residual risk.

## Concrete Steps

1. Merge PR refs in order: `#20`, `#23`, `#22`, `#21`, `#24`.
2. Resolve duplicate `rule` doc type entries and import readability findings.
3. Run focused `rg` checks for `rule`, `doc_type: rule`, and
   `changed_files.txt`.
4. Run required gates.
5. Update workbench evidence and PR body.
6. Run independent auditor.
7. If auditor fails, route concrete cleanup back to build/test/docs owner.
8. Commit/PR only after auditor returns no pending work.

## Progress

- Merge and code cleanup: complete.
- Validation gates: complete.
- Workbench documentation refresh: complete in doc-updater pass.
- First independent audit: failed because validation generated unscoped
  `docs/arc`, `docs/knowledge`, and `docs/map` drift.
- Corrective build pass: restored unscoped docs drift and updated workbench to
  reflect safe PR route.
- Final independent audit: executed by Gibbs; failed only stale workbench metadata. This doc-updater pass corrected stale metadata, active-session pointer, and evidence as E-20260604225750742719142.

## Validation and Acceptance

- Focused `rg` checks for `rule`, `doc_type: rule`, and `changed_files.txt`.
- `just lint-scripts`
- `just test-scripts-all`
- `just lint-runtime`
- `just test-runtime`
- `just lint`
- `just agents-all`
- `git diff --check`
