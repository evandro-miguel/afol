---
doc_type: plan
id: 260528_0930_f05-review-fix_plan_01
theme: f05-review-fix
status: final
owners:
- orchestrator
workstream_intent: delivery
artifact_purpose: Define the concrete execution path for work that will actually be performed.
created_at: 2026-05-28T09:30:43-03:00
updated_at: '2026-05-28T09:45:05-03:00'
roadmap_feature: F-05
parent_spec: 260521_0050_smart-rules-and-skills-routing_spec_01
child_spec:
links:
  roadmap: docs/arc/GENERAL-ROADMAP.md
  task: 260528_0930_f05-review-fix_task_01
  brainstorm:
  explorer_check:
  research:
  postmortem:
repo: agentic_start_folder_dev_refactor_TS
branch: dev_refactor_TS
output_artifacts:
  primary:
    plan: 260528_0930_f05-review-fix_plan_01
    task: 260528_0930_f05-review-fix_task_01
  sidecars:
    brainstorm:
    research:
    explorer_check:
    postmortem:
  sidecar_justification:
    brainstorm: not_required
    research: not_required
    explorer_check: not_required
    postmortem: not_required
---

# Plan: f05-review-fix

## Purpose / Big Picture

- Close the F-05 review blockers without changing unrelated scaffold behavior.
- Make the live scaffold and `src/project-template/.agents/**` mirror the same rule-routing and skills-metadata contract.
- A user can see the result by running governed feature commands and `skills-sync update-metadata` with the new explicit create gate.

## Progress

- [x] 2026-05-28 12:30Z - Opened the review-fix session and started the governed task.
- [x] 2026-05-28 12:43Z - Implemented the F-05 runtime and template parity changes.
- [x] 2026-05-28 12:45Z - Passed root/template targeted tests and repo validation gates.
- [x] 2026-05-28 12:50Z - Finalized the session report and evidence chain.

## Surprises & Discoveries

- Observation: The template copy was still on the pre-F-05 surface and lacked rule catalog / metadata-command parity.
  Evidence: `src/project-template/.agents/scripts/lib/execution_commands.py` and `src/project-template/.agents/scripts/agents-skills-sync.py` needed the missing F-05 blocks.
- Observation: The live plan and task artifacts needed direct updates before strict session verification could pass.
  Evidence: `verify-tasks --strict` rejected the session while the plan still contained template placeholders.

## Decision Log

- Decision: Optional feature-rule misses now warn and skip; only mandatory/required rules fail the preflight.
  Rationale: Keeps heuristically selected stale rules from hard-failing while preserving fail-fast for required governance surfaces.
  Date/Author: 2026-05-28 / builder
- Decision: `skills-sync update-metadata` now rejects unknown skills unless `--create` is explicit.
  Rationale: Prevents phantom metadata on typos while preserving an intentional creation path.
  Date/Author: 2026-05-28 / builder
- Decision: Mirror F-05 runtime, tests, and rule catalog into `src/project-template/.agents/**`.
  Rationale: Bootstrap copies must reflect the same surface contract as the live scaffold.
  Date/Author: 2026-05-28 / builder

## Outcomes & Retrospective

- Outcome: Root and template now share the F-05 rule-routing and skills-metadata surfaces, and the review findings are covered by tests.
- Remaining: Normalize the active-session pointer and leave the worktree clean after commit.
- Lesson: Template parity needs explicit tests, not just code mirroring.

## Governance Context

- Roadmap feature: `F-05`
- Parent spec: `260521_0050_smart-rules-and-skills-routing_spec_01`
- Child spec: ``
- Planning rule:
  - Do not redefine feature philosophy here; this file records the execution path already approved for F-05.
  - Keep the scope limited to the review findings and the mirrored template surfaces.

## Planning Inputs

- Task artifact: `260528_0930_f05-review-fix_task_01` (required)
- Brainstorm artifact: ``
- Explorer check artifact: ``
- Research artifact: ``
- Postmortem artifact: ``
- Knowledge lookup performed:
  - `rg` and targeted reads on `.agents/scripts/lib/execution_commands.py`, `.agents/scripts/agents-skills-sync.py`, and the corresponding tests

## Context and Orientation

- `./.agents/scripts/lib/execution_commands.py` loads governed feature bundles and now warns when optional rules are missing.
- `./.agents/scripts/agents-skills-sync.py` manages project skills metadata and now requires `--create` for unknown skills.
- `src/project-template/.agents/**` is the bootstrap mirror that must stay aligned with the live surfaces.

## Scope

- In scope:
  - `./.agents/scripts/lib/execution_commands.py`
  - `./.agents/scripts/agents-implement.py`
  - `./.agents/scripts/agents-skills-sync.py`
  - `./.agents/scripts/tests/test_execution_command_scenarios.py`
  - `./.agents/scripts/tests/test_agents_skills_sync.py`
  - `src/project-template/.agents/**` parity for the same surfaces
- Out of scope:
  - F-06+ roadmap features
  - new workbench framework behavior

## Plan of Work

- Patch the governed feature resolver in `./.agents/scripts/lib/execution_commands.py` so optional rule gaps warn-and-skip while mandatory gaps still fail.
- Patch `./.agents/scripts/agents-implement.py` to surface skipped-rule warnings during governed preflight.
- Patch `./.agents/scripts/agents-skills-sync.py` so `update-metadata` refuses unknown skills unless `--create` is explicit.
- Mirror the same runtime, test, and rule catalog surfaces into `src/project-template/.agents/**`.

## Concrete Steps

1. Update `./.agents/scripts/lib/execution_commands.py`, `./.agents/scripts/agents-implement.py`, and `./.agents/scripts/agents-skills-sync.py` with the F-05 review fixes.
2. Mirror the same behavior into `src/project-template/.agents/scripts/**` and `src/project-template/.agents/rules/**`.
3. Run the focused root/template pytest files, then `just lint`, `just lint-scripts`, `just test-scripts-all`, `bun run typecheck`, `bun test`, and `git diff --check`.

## Interfaces and Dependencies

- Tools:
  - `just`
  - `bun`
  - `./.agents/scripts/.venv/bin/python`
- MCPs:
  - None for the code change itself
- Skills:
  - `agentic-folder-sys`
  - `code-discovery`
- Files and interfaces that must exist at the end:
  - `./.agents/scripts/lib/execution_commands.py::load_feature_operation_governance`
  - `./.agents/scripts/agents-skills-sync.py::cmd_update`
  - `src/project-template/.agents/rules/index.json`

## Risks and Mitigations

- Risk: Template and live scaffold drift apart again -> Mitigation: keep tests and rule catalog files mirrored for the F-05 surfaces.
- Risk: Unknown-skill metadata edits reintroduce phantom entries -> Mitigation: keep `--create` explicit and test the negative path.

## Validation and Acceptance

- Unit: `PYTHONHASHSEED=0 .agents/scripts/.venv/bin/python -m pytest -q .agents/scripts/tests/test_execution_command_scenarios.py .agents/scripts/tests/test_agents_skills_sync.py` -> pass
- Unit: `PYTHONHASHSEED=0 .agents/scripts/.venv/bin/python -m pytest -q src/project-template/.agents/scripts/tests/test_execution_command_scenarios.py src/project-template/.agents/scripts/tests/test_agents_skills_sync.py` -> pass
- Typecheck: `bun run typecheck` -> pass
- Lint: `just lint` and `just lint-scripts` -> pass
- Behavioral acceptance:
  - Optional missing rule surfaces warn-and-skip in governed preflight.
  - `update-metadata` fails on unknown skills unless `--create` is explicit.
  - Bootstrap template files mirror the same rule/metadata contract.

## Idempotence and Recovery

- The focused pytest commands are safe to rerun.
- If a validation gate fails, fix the smallest shared root cause and rerun the narrowest failing command before broadening back out.

## Artifacts and Notes

- Session report: `./.agents/wb/260528_0930_f05-review-fix/260528_0930_f05-review-fix_report_01.md`
- Evidence trail: current task ledger and the strict verification outputs captured in this session.

## Completion Gate

- [x] Task exists and tracks the executable work
- [x] No step exists only to make another plan or do generic research
- [x] Relevant prior knowledge was searched or explicitly ruled out
- [x] Any optional artifact created for this workstream is final
- [x] The ExecPlan remains self-contained enough for a new contributor to resume
- [x] Progress entries reflect the actual current state
- [x] Validation path is concrete enough to execute without guesswork
