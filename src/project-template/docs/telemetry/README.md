# Telemetry

Telemetry is experimental. The template only ships a schema under
`.afol/data/telemetry/schemas/`. Do not assume events are collected
automatically.

If this project later enables `afol telemetry`, treat that command as
changeable. Until then, ignore this directory.
