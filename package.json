{
  "name": "agentic-start-folder-ts-kernel",
  "private": true,
  "version": "0.0.0",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14"
  },
  "bin": {
    "afol": "./afol"
  },
  "scripts": {
    "dev": "bun run cli/main.ts",
    "template:generate": "bun run cli/dev/generate-template.ts",
    "template:check": "bun test cli/tests/template-policy.test.ts cli/tests/bootstrap-template-cleanliness.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint:biome": "biome lint cli",
    "lint:oxlint": "oxlint cli",
    "lint:knip": "knip --dependencies --use-tsconfig-files --max-issues 20",
    "toolchain:diff": "bun run cli/dev/toolchain-smoke.ts",
    "validate:toolchain": "bun run lint:biome && bun run lint:oxlint && bun run lint:knip && bun run toolchain:diff",
    "test": "bun test",
    "test:unit": "bun test cli/tests",
    "test:ci": "AGENT=1 bun test --concurrent --max-concurrency 4 --bail=1",
    "validate": "bun run typecheck && bun test",
    "typecheck:ts7:informative": "if bunx --bun typescript@next tsc --noEmit --project tsconfig.json; then exit 0; else echo 'TypeScript 7 lane not available or not safe to run locally'; fi",
    "validate:template": "bun run template:generate && bun test cli/tests/template-policy.test.ts cli/tests/bootstrap-template-cleanliness.test.ts",
    "validate:bootstrap": "bun test cli/tests/bootstrap-template-cleanliness.test.ts cli/tests/bootstrap.test.ts cli/tests/bootstrap-conflicts.test.ts",
    "kernel": "bun run cli/main.ts",
    "build": "bun build --compile ./cli/main.ts --outfile dist/afol",
    "build:deterministic": "bun install --frozen-lockfile && bun run build",
    "validate:security": "bun run security:scan:informative",
    "security:deps:informative": "bun run cli/dev/security-scan.ts deps",
    "security:secrets:informative": "bun run cli/dev/security-scan.ts secrets",
    "security:scan:informative": "bun run security:deps:informative && bun run security:secrets:informative",
    "validate:release": "bun run validate:toolchain && bun run validate:template && bun run validate:bootstrap && bun run build:deterministic && bun run smoke:dist && bun run validate:security",
    "smoke:dist": "bun run build && ./dist/afol --help > /dev/null",
    "smoke:clean": "bash -lc 'set -euo pipefail; tmpdir=$(mktemp -d); trap \"rm -rf \\\"$tmpdir\\\"\" EXIT; checkout=\"$tmpdir/checkout\"; mkdir -p \"$checkout\"; tar -C \"$PWD\" --exclude .git --exclude node_modules --exclude dist --exclude .bun-build\\* -cf - . | tar -C \"$checkout\" -xf -; cd \"$checkout\"; bun install --frozen-lockfile; bun run build; ./dist/afol --help > /dev/null'"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/bun": "^1.3.2",
    "citty": "^0.2.2",
    "diff": "^9.0.0",
    "jsdiff": "^1.1.1",
    "knip": "^6.15.0",
    "oxlint": "^1.67.0",
    "typescript": "^6.0.3",
    "valibot": "^1.4.1"
  }
}
