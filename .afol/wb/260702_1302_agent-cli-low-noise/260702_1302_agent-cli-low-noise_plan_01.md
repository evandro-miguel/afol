# Plan: agent-cli-low-noise

- Created by native CLI workbench lifecycle.

## Native command metadata
- task: Make routine AFOL CLI commands low-noise and agent-safe
- task: Harden AFOL mutation/update governance and session creation
- task: Add structured JSON contracts for agent-facing commands
- task: Remove project-benchmark runtime inference drift

## Execution Plan

- T-01: Make routine AFOL CLI commands low-noise and agent-safe
- T-02: Harden AFOL mutation/update governance and session creation
- T-03: Add structured JSON contracts for agent-facing commands
- T-04: Remove project-benchmark runtime inference drift
- Keep edits scoped to the task and repository rules.
- Record evidence before marking the task done.

## Validation

- Run the command named in the task or governing brief.
- Capture the validation result in the evidence ledger.

## Closure Criteria

- Every task is marked done only after passed evidence exists.
- Delivery notes identify the changed files and verification result.
