---
doc_type: plan
id: 260604_2243_merge-pr20-pr24_plan_01
theme: merge-pr20-pr24
status: final
owners:
- build
workstream_intent: delivery
artifact_purpose: Execute integration of upstream PRs 20-24.
created_at: 2026-06-04 22:43:46-03:00
updated_at: '2026-06-04T22:57:50-03:00'
roadmap_feature: F-11
parent_spec: 260521_0110_validation-ci-and-benchmarks_spec_01
links:
  task: 260604_2243_merge-pr20-pr24_task_01
repo: agentic-standard-folder
branch: main_dev_merge/pr20-pr24
---

# Plan: merge PRs 20-24

## Purpose / Big Picture

Integrate upstream PRs `#20` through `#24` into the executor branch, preserve
the validation and documentation contracts affected by those merges, and leave
auditable workbench evidence for the safe PR route.

## Objective

- Integrate upstream PRs `#20` through `#24` into
  `main_dev_merge/pr20-pr24` with conflict cleanup, full repo gates, workbench
  evidence, and reusable PR body.

## Facts

- Base branch: `origin/main`.
- Executor branch: `main_dev_merge/pr20-pr24`.
- PR refs fetched: `origin/pr/20` through `origin/pr/24`.
- Workbench CLI failed before session creation because local contract rejects
  `F-pr20-pr24` and expects `^F-[0-9]{2,3}$`.
- The finalized governance link is `F-11` with parent spec
  `260521_0110_validation-ci-and-benchmarks_spec_01`, because the delivered
  work was an integration and validation-gate hardening pass.

## Context and Orientation

This was a factory-repo integration session. The work merged upstream PR refs,
resolved semantic cleanup around doc linting and runtime/template state, and
avoided pushing directly to `main_dev` after detecting remote divergence.

## Scope

- Merge PRs in order: `#20`, `#23`, `#22`, `#21`, `#24`.
- Resolve doc type rule conflicts and stale references.
- Run focused checks and required gates.
- Commit local integration only after final audit passes. Create PR from
  `main_dev_merge/pr20-pr24` to `main`; do not push to `main_dev`.

## Success Criteria

- PRs `#20`, `#21`, `#22`, `#23`, and `#24` are represented in the integration
  branch.
- `VALID_DOC_TYPES` contains one `rule` entry in both mirrored lint scripts.
- Rule markdown frontmatter includes `doc_type: rule` where applicable.
- `changed_files.txt` is removed and has no live repo references.
- Required gates pass.
- Workbench report and PR body summarize changes, gates, and residual risk.

## Plan of Work

The execution path was merge-first and gate-driven: fetch and merge the PR refs,
resolve compatibility drift, run focused searches for known conflict vectors,
run the required validation gates, refresh workbench artifacts, then publish the
executor branch only after review.

## Concrete Steps

1. Merge PR refs in order: `#20`, `#23`, `#22`, `#21`, `#24`.
2. Resolve duplicate `rule` doc type entries and import readability findings.
3. Run focused `rg` checks for `rule`, `doc_type: rule`, and
   `changed_files.txt`.
4. Run required gates.
5. Update workbench evidence and PR body.
6. Run independent auditor.
7. If auditor fails, route concrete cleanup back to build/test/docs owner.
8. Commit/PR only after auditor returns no pending work.

## Progress

- [x] Merge and code cleanup: complete.
- [x] Validation gates: complete.
- [x] Workbench documentation refresh: complete in doc-updater pass.
- [x] First independent audit: failed because validation generated unscoped
  `docs/arc`, `docs/knowledge`, and `docs/map` drift.
- [x] Corrective build pass: restored unscoped docs drift and updated workbench to
  reflect safe PR route.
- [x] Final independent audit: executed by Gibbs; failed only stale workbench
  metadata. The doc-updater pass corrected stale metadata, active-session
  pointer, and evidence as `E-20260604225750742719142`.

## Surprises & Discoveries

- The initial generated feature/spec identifiers were not valid under the
  workbench governance contract.
- The attempted direct push to remote `main_dev` was rejected as
  non-fast-forward, so publication moved to the executor branch.

## Decision Log

- Use the executor branch `main_dev_merge/pr20-pr24` as the safe publication
  path instead of force-updating `main_dev`.
- Treat the invalid original `F-pr20-pr24` / `merge-prs` identifiers as
  historical context and bind the final artifact to the existing validation
  feature/spec.

## Idempotence and Recovery

Re-running the validation commands is safe. If the merge branch must be rebuilt,
start from `origin/main`, re-fetch PR refs `20` through `24`, repeat the merge
order in this plan, and compare the final focused `rg` checks and gate results
against the evidence below.

## Artifacts and Notes

- Task: `260604_2243_merge-pr20-pr24_task_01.md`.
- Report: `260604_2243_merge-pr20-pr24_report_01.md`.
- Log: `260604_2243_merge-pr20-pr24_log_01.md`.
- Evidence ledger: `.evidence.jsonl`.

## Interfaces and Dependencies

- Git remotes: `origin/main`, `origin/pr/20`, `origin/pr/21`, `origin/pr/22`,
  `origin/pr/23`, `origin/pr/24`, and executor branch
  `main_dev_merge/pr20-pr24`.
- Validation surfaces: legacy `just` gates used by this historical session,
  doc lint scripts, runtime tests, and `git diff --check`.

## Validation and Acceptance

- Focused `rg` checks for `rule`, `doc_type: rule`, and `changed_files.txt`.
- `just lint-scripts`
- `just test-scripts-all`
- `just lint-runtime`
- `just test-runtime`
- `just lint`
- `just agents-all`
- `git diff --check`

## Outcomes & Retrospective

The integration branch was completed, validated, documented, and published to
the safe executor branch. The residual issue was governance metadata drift in
the archived plan format, now corrected to the current strict verifier
contract.
